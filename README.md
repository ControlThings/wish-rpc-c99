# Wish RPC implentation

Warning: This code cannot be built on its own. It only works when included from projects that used this code before it was extracted.

This is a generic BSON based RPC implementation. 
